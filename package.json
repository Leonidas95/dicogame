{
  "name": "dicogame",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.5.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@7.12.1",
  "volta": {
    "node": "18.9.1"
  }
}
